name: Pull request

on:
  pull_request:

jobs:
  audit:
    uses: ./.github/workflows/actions/audit.yml

  test:
    uses: ./.github/workflows/actions/test.yml
    needs: audit

  wasm-check:
    uses: ./.github/workflows/actions/wasm-check.yml
    needs: audit
