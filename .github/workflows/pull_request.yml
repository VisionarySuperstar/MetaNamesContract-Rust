name: Pull request

on:
  pull_request:

jobs:
  audit:
    extend: ./actions/audit.yml

  test:
    extend: ./actions/test.yml
    needs: audit

  wasm-check:
    extend: ./actions/wasm-check.yml
    needs: audit
