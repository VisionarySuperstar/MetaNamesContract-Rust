name: Pull request

on:
  pull_request:

jobs:
  audit:
    uses: ./.github/workflows/audit.yml

  test:
    uses: ./.github/workflows/test.yml
    needs: audit

  wasm-check:
    uses: ./.github/workflows/wasm-check.yml
    needs: audit
